PATH = '/bower_components'

require.config
  paths:
    jquery: "#{PATH}/jquery/dist/jquery"
    backbone: "#{PATH}/backbone/backbone"
    underscore: "#{PATH}/lodash/dist/lodash"
    fabric: "#{PATH}/fabric/dist/fabric"
